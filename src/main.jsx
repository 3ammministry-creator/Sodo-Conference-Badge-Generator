import React from 'react';
import ReactDOM from 'react-dom/client';
import BadgeGenerator from '../BadgeGenerator.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BadgeGenerator />
  </React.StrictMode>
);
